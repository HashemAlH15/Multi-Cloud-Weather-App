# Terraform state files — contain secrets, never commit
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform variable values — contain credentials
*.tfvars
*.tfvars.json

# Terraform working directories
.terraform/
.terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config
.terraformrc
terraform.rc
